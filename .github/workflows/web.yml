
name: EC-Web

on:
  push:
    branches:
      - dev

jobs:
  
    runs-on: ubuntu-latest
    steps:
      - name: Get OS Info
        run: | 
         uname -a
         sudo apt update && sudo apt upgrade
      - name: get users
        run: cat /etc/passwd
